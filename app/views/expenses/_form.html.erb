<%= form_for object, url: expenses_path, method: :post  do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <%= f.label t(".description") %>
  <%= f.text_field :description %>

  <%= f.label t(".amount") %>
  <%= f.text_field :amount %>

  <%= f.submit "Gửi", class: "btn btn-primary" %>
<% end %>
