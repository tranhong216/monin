<% provide :title, "Lịch sử tiền đi" %>
<div id="main">
  <h1>Tiền đi tháng này như thế nào ?</h1>

  <ul class="users">
    <table>
      <tr>
        <th>Tên</th>
        <th>Ngày</th>
        <th>Mua gì</th>
        <th>Tổng</th>
        <% if current_user.admin? %>
          <th>Tool</th>
        <% end %>
      </tr>
      <%= render @expenses %>
    </table>
  </ul>

</div>
